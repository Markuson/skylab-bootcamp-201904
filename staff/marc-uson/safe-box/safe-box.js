'use strict';
/**
 * if only is passed the correct password, should return true and show the saved parameter.
 * Should return false if the pasword is not correct 
 * 
 * If a pasword and argument1 is passed, should add argument1 in the safeBox if the password is correct
 * and throw an Error if the pasword is not correct.
 * 
 * if password, argument1 is passed, and changePass is true. if password is correct, should change the 
 * value of the saved
 * 
 * @param {String} password 
 * @param {*} argument1 
 * @param {Boolean} changePass 
 */

function safeBox(password, argument1, changePass){
    //haz algo
}